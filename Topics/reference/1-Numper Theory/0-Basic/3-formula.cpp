/**
 * Fibonacci numbers: 0, 1, 1, 2, 3, 5
 * f(n) = f(n - 1) + f(n - 2)
 * f(n) = ( (1 + sqrl(5))^n - (1 - sqrl(5))^n ) / (2^n * sqrl(5))
*/


/**
 * Logarithms: logk(x) = a --> k^a = x.
 * logk(ab) = logk(a) + logk(b).
 * logk(x^n) = n·logk(x).
 * logk(a/b)= logk(a) − logk(b).
 * logu(x) =logk(x) / logk(u).
 * the number of digit of x in base b = floor(logb(x)+1).
 * 
*/