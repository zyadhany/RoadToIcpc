/**
 * Subarray sum: array of n positive integers and a target sum x
 * using the two pointers, On each turn: the left moves one step
   to the right, and the right moves to the right as long as the
   resulting subarray sum is at most x.
 */

/**
 * 2SUM problem:array of n numbers and a target sum x,
   find two array values such that their sum is x,
 * we first sort the array values in increasing order. After
   that, we iterate through the array using two pointers.
   The left pointer starts at the first value and moves one
   step to the right on each turn. The right pointer
   begins at the last value and always moves to the 
   left until the sum of the left and right value is at most x
 */

