
   list<int> l={1,2,3};

   l.remove(2);

next(it)

    auto it=l.begin();
    it++;
    l.erase(it,l.end());

 it=l.erase(it, next(it,2));


auto it=l.begin();
    it++;
    l.insert(it,2,50);


    l.assign(10,2);

    l1.merge(l2);

    l1.splice(l1.begin(),l2);

    l1.unique();


  bool evenNum(ll n)
   {
    if (n%2==0)return true;
    else return false;
    }
    l1.remove_if(evenNum);
     هيسمح كل الزوجي  


l1.sort();
l1.sort(greater<int>());

auto it=l1.begin();
    it++;
    *it=5;

